# 技术报告

## 1. 项目概述

本项目是一个基于 Python 的 Web 应用，用于生成 Garmin FIT 格式的运动数据文件。用户通过浏览器填写运动参数（编号、日期、开始时间、时长），系统基于真实 GPS 轨迹模板生成包含完整运动数据的 `.fit` 文件，可导入 Keep、Garmin Connect 等运动 APP。

### 1.1 适用场景

- 校园跑步数据生成
- 运动 APP 数据导入
- FIT 文件格式研究

### 1.2 设计目标

- 轻量级：仅一个第三方依赖
- 易部署：单命令启动，无需数据库配置
- 真实性：GPS 轨迹来自真实跑步数据，运动参数符合中国健康标准

---

## 2. 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Python `http.server` | 标准库，无需额外安装 |
| 并发模型 | `socketserver.ThreadingMixIn` | 多线程处理并发请求 |
| 数据库 | SQLite | Python 内置，文件级数据库 |
| FIT 生成 | `fit-tool` (0.9.15) | Garmin FIT 格式读写库 |
| 前端 | HTML5 + CSS3 + JavaScript | 原生实现，零框架 |
| UI 风格 | Glassmorphism | 液态玻璃效果 + 深色模式 |

---

## 3. 系统架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────┐
│              浏览器 (原生 HTML/CSS/JS)             │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │ 数据填写  │  │ 数据下载  │  │  公告系统     │  │
│  └──────────┘  └──────────┘  └──────────────┘  │
└────────────────────┬────────────────────────────┘
                     │ HTTP (JSON + 二进制流)
                     ▼
┌─────────────────────────────────────────────────┐
│       ThreadedHTTPServer (端口 5005)              │
│  ┌───────────────────────────────────────────┐  │
│  │  Handler (do_GET / do_POST / do_OPTIONS)  │  │
│  └───────────────────────────────────────────┘  │
└──────┬──────────────┬──────────────┬────────────┘
       │              │              │
       ▼              ▼              ▼
┌──────────┐  ┌──────────────┐  ┌──────────┐
│  SQLite  │  │ FIT 生成器    │  │ 公告文件  │
│ 活动记录  │  │ generate_fit │  │ Call.txt │
│ 下载记录  │  └──────────────┘  └──────────┘
└──────────┘
```

### 3.2 各层职责

| 层级 | 职责 |
|------|------|
| 前端 | 用户交互、表单验证、API 调用、文件下载、公告展示 |
| HTTP 服务器 | 请求路由、静态文件服务、API 处理、CORS 支持 |
| 数据层 | SQLite 数据库操作，线程安全锁管理 |
| 生成层 | FIT 文件生成，GPS 轨迹处理，运动数据计算 |

---

## 4. 目录结构

```
V2O/
├── backend/
│   └── server.py              # HTTP 服务器主程序 (220行)
├── frontend/
│   └── form.html              # 前端单页应用 (394行)
├── tools/
│   └── generate_fit.py        # FIT 文件生成器 (213行)
├── data/
│   ├── activities.db           # SQLite 数据库
│   ├── standard.fit            # GPS 轨迹模板
│   └── Call.txt                # 公告内容
├── requirements.txt
├── LICENSE
├── .gitignore
├── README.md
└── 技术报告.md
```

---

## 5. 后端模块分析

### 5.1 文件：`backend/server.py`（220行）

#### 服务器配置

| 配置项 | 值 |
|--------|-----|
| 端口 | 5005 |
| 数据库路径 | `data/activities.db` |
| 并发模型 | `ThreadingMixIn` + `HTTPServer` |
| 线程模式 | `daemon_threads = True` |

#### 启动流程

```python
def main():
    os.chdir(project_root)   # 切换到项目根目录
    init_db()                # 初始化数据库
    server.serve_forever()   # 启动服务器
```

#### API 端点

**GET `/api/activities`**
- 返回所有活动记录 JSON 数组
- 按 `created_at` 降序排列

**GET `/api/announcement`**
- 读取 `data/Call.txt`
- 返回 `{ version: "V1.30.1", content: "..." }`
- 首行为版本号，其余为正文

**POST `/api/submit`**
- 请求：`{ user_id, date, start_time, duration }`
- 验证必填参数 → 插入 `activities` 表
- 记录访客 IP（`self.client_address[0]`）
- 返回：`{ success: true, id: N }`

**POST `/api/generate`**
- 请求：`{ user_id, date, start_time, duration }`
- 调用 `generate_fit()` 生成 FIT 文件
- 以二进制流返回文件
- 生成成功后记录到 `downloads` 表
- 删除临时文件

#### 数据库操作

所有数据库操作通过 `db_lock`（`threading.Lock()`）保护，确保线程安全。

```python
def init_db():
    # 创建 activities 表和 downloads 表
    with db_lock:
        conn.execute('CREATE TABLE IF NOT EXISTS activities ...')
        conn.execute('CREATE TABLE IF NOT EXISTS downloads ...')

def insert_activity(user_id, date, start_time, duration, visitor_ip):
    # 插入活动记录，返回自增 ID
    with db_lock:
        ...

def get_activities():
    # 查询所有活动记录
    with db_lock:
        ...

def insert_download(user_id, visitor_ip):
    # 记录下载操作
    with db_lock:
        ...
```

#### CORS 配置

```python
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: POST, GET, OPTIONS
Access-Control-Allow-Headers: Content-Type
```

#### 异常处理

- 所有 POST 请求在 `try-except` 中处理
- 异常时返回 500 状态码和错误 JSON
- 错误信息打印到控制台

---

## 6. 前端模块分析

### 6.1 文件：`frontend/form.html`（394行）

单页应用，包含数据填写、数据下载、公告系统三个功能模块，通过 Tab 切换。

#### UI 设计

- **玻璃拟态**：`backdrop-filter: blur(40px) saturate(1.4)` + 半透明背景
- **深色模式**：`@media (prefers-color-scheme: dark)` 完整适配
- **CSS 变量**：50+ 个自定义属性，覆盖亮暗主题所有颜色
- **动画**：卡片入场、Tab 切换、按钮缩放、脉冲效果、骨架屏、行入场
- **光源动画**：三个漂浮光球，布朗运动算法随机漫步，碰边界触发亮度脉冲

#### 表单验证

| 字段 | 正则 | 说明 |
|------|------|------|
| `user_id` | `/^\d{3}$/` | 三位数字 |
| `date` | `/^\d{8}$/` | 八位数字 |
| `start_time` | `/^\d{4}$/` | 四位数字 |
| `duration` | `/^\d{4}$/` | 四位数字 |

#### 快速提交逻辑

- **编号**：从 Cookie 读取，首次默认 `666`
- **日期/时间**（北京时间）：
  - 15:00-23:59 → 当天日期，14:30 ± 1小时
  - 00:00-14:59 → 前一天日期，22:30 ± 1小时
- **时长**：19:10 ~ 20:53 随机

#### Cookie 管理

| Cookie | 用途 | 有效期 |
|--------|------|--------|
| `uid` | 记忆用户编号 | 365天 |
| `ann_ver` | 记录已读公告版本 | 365天 |

#### 公告系统

- 左上角浮动按钮，新公告时黄色呼吸灯闪烁
- 点击展开全屏遮罩 + 居中面板
- 遮罩层 `blur(40px)` 高斯模糊
- 版本号对比：Cookie 中无记录或版本不一致时闪烁

---

## 7. FIT 文件生成器分析

### 7.1 文件：`tools/generate_fit.py`（213行）

#### 核心函数：`generate_fit(user_id, date, start_time, duration, output_path=None)`

**参数解析**
- `date` (YYYYMMDD) → year, month, day
- `start_time` (HHMM) → hour, minute
- `duration` (MMSS) → total_duration（秒）

**GPS 轨迹**
- 从 `data/standard.fit` 模板读取 1013 个 GPS 坐标点
- 坐标为度数格式，直接使用不做转换
- 模块加载时读取一次，后续复用

**随机距离**：2.95 ~ 3.20 km（`random.uniform`）

**步频参数**：基础 165-175 spm，每条记录 ±5 扰动

#### 生成的 FIT 文件结构

| 消息类型 | 内容 |
|----------|------|
| FileIdMessage | 文件类型 ACTIVITY，制造商 GARMIN，产品 Forerunner 245 |
| DeviceInfoMessage | 设备信息，软件版本 7.2 |
| EventMessage | TIMER/START 开始事件 |
| RecordMessage ×1013 | 每个 GPS 轨迹点：时间戳、经纬度、距离、步频、速度、总周期数、步长 |
| LapMessage ×N | 每圈 400m：圈时间、圈距离、步频、步长 |
| SessionMessage | 运动汇总：总时长、总距离、卡路里、步频、速度、功率、触地时间、步长 |
| EventMessage | TIMER/STOP_ALL 结束事件 |
| ActivityMessage | 活动记录，会话数 1 |

#### 运动参数范围（参考中国健康标准）

| 指标 | 范围 | 来源 |
|------|------|------|
| 平均步频 | 165-175 spm | 《国民体质测定标准》 |
| 步频扰动 | ±5 spm | 自然波动 |
| 平均功率 | 220-280 W | 成年男性跑步 |
| 平均触地时间 | 210-250 ms | 普通跑者 |
| 卡路里 | 距离(km) × 70 kcal | 运动医学估算 |
| 每圈距离 | 400 m | 标准跑道 |

#### 设备模拟

```python
FileIdMessage:
    manufacturer = Manufacturer.GARMIN
    product = 3843
    product_name = 'Forerunner 245'

DeviceInfoMessage:
    manufacturer = Manufacturer.GARMIN
    software_version = 7.2
    hardware_version = 1.0
    source_type = 1  # ANT+
```

---

## 8. 数据库设计

### 8.1 activities 表

```sql
CREATE TABLE activities (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,          -- 用户编号
    date TEXT NOT NULL,             -- 日期 (YYYYMMDD)
    start_time TEXT NOT NULL,       -- 开始时间 (HHMM)
    duration TEXT NOT NULL,         -- 运动时长 (MMSS)
    visitor_ip TEXT,                -- 访客 IP 地址
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

### 8.2 downloads 表

```sql
CREATE TABLE downloads (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,          -- 用户编号
    visitor_ip TEXT,                -- 访客 IP 地址
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

---

## 9. 数据流程

### 9.1 提交流程

```
用户填写表单 → 前端验证 → POST /api/submit
    → 验证必填参数
    → 获取访客 IP
    → INSERT INTO activities
    → 返回 { success: true, id }
    → 前端自动跳转下载页
```

### 9.2 下载流程

```
用户点击下载 → POST /api/generate
    → 验证必填参数
    → 调用 generate_fit()
        → 读取 standard.fit 模板
        → 提取 GPS 轨迹点
        → 生成随机距离 (2.95-3.20km)
        → 创建 RecordMessage (GPS + 步频 + 速度)
        → 创建 LapMessage (每圈 400m)
        → 创建 SessionMessage (运动汇总)
        → 构建 FIT 文件
    → 返回二进制流
    → INSERT INTO downloads
    → 删除临时文件
    → 浏览器触发下载
```

---

## 10. 公告系统

### 10.1 文件格式

`data/Call.txt` 首行为版本号，其余为公告正文：

```
V1.30.1
公告内容第一行
公告内容第二行
```

### 10.2 版本对比逻辑

1. 前端请求 `/api/announcement`
2. 后端解析首行为 `version`，其余为 `content`
3. 前端对比 `version` 与 Cookie 中的 `ann_ver`
4. 无 Cookie 或版本不一致 → 按钮闪烁
5. 点击查看 → 写入 Cookie，停止闪烁

---

## 11. 安全与配置

### 11.1 CORS

全开放模式（`Allow-Origin: *`），适用于开发和局域网场景。

### 11.2 线程安全

数据库操作通过 `threading.Lock()` 保护，每次操作获取锁 → 创建连接 → 执行 → 释放。

### 11.3 文件安全

- 生成的 FIT 文件以 `{user_id}.fit` 命名
- 文件发送后立即删除
- 输出路径限制在 `data/` 目录下

---

## 12. 依赖项

### 12.1 第三方库

| 库 | PyPI 包名 | 版本 | 用途 |
|----|-----------|------|------|
| fit-tool | `fit-tool` | 0.9.15 | Garmin FIT 格式文件读写 |

安装：`pip install fit-tool`

### 12.2 标准库

| 模块 | 用途 |
|------|------|
| `http.server` | HTTP 服务器 |
| `socketserver` | 多线程支持 |
| `json` | JSON 序列化 |
| `sqlite3` | 数据库操作 |
| `threading` | 线程锁 |
| `os` | 文件和路径操作 |
| `sys` | 模块路径管理 |
| `random` | 随机数生成 |
| `datetime` | 时间处理 |

---

## 13. 性能分析

### 13.1 并发模型

- `ThreadingMixIn`：每个请求在独立线程中处理
- 支持多设备同时访问
- `daemon_threads = True`：主线程退出时自动终止子线程

### 13.2 数据库

- SQLite 文件级数据库，适合中小规模使用
- `db_lock` 保护并发写入
- 每次操作创建新连接（简单但有开销）

### 13.3 FIT 生成

- 模板文件加载一次（模块级缓存）
- 单次生成约 1013 个 RecordMessage
- 生成后立即写入磁盘并返回

---

## 14. 优缺点总结

### 优点

- 依赖极少，仅需一个第三方库
- 部署简单，单命令启动
- 前端零框架，纯原生实现
- 支持深色模式和响应式布局
- GPS 轨迹来自真实数据
- 运动参数符合健康标准

### 缺点

- 无身份认证机制
- 后端输入验证不充分
- 无日志记录系统
- 数据库连接未复用
- 无配置文件，端口和路径硬编码
- 无单元测试

---

## 15. 改进建议

### 功能增强

- 添加用户注册/登录
- 支持多模板管理
- 批量生成功能
- 运动数据统计图表

### 运维改进

- 添加 logging 模块
- 配置文件管理（端口、路径）
- Docker 容器化部署
- HTTPS 支持

### 代码质量

- 添加类型注解
- 完善文档字符串
- 添加单元测试
- 数据库连接池

---

*报告版本：V2*
*生成时间：2026-06-12*
