# 技术报告

*版本：V3 ｜ 更新日期：2026-09-09 ｜ 对应版本：V2.0.0beta*

## 1. 项目概述

本项目是一个基于 Python 的 Web 应用，用于生成 Garmin FIT 格式的运动数据文件。用户通过浏览器选择/填写运动参数（编号、日期、开始时间、时长、距离档位），系统以 `data/standard.fit` 为母版**拟合"胶囊形跑道"（两条直道 + 两端半圆）**，按所选距离绕圈重新采样生成全新 GPS 轨迹，并叠加速度曲线（3km 档为**快跑段 + 中段慢走 3~4km/h 的强变化剖面**、5km+ 平缓递减）与慢跑动力学人设，输出兼容 Keep、Garmin Connect 等运动 APP 的 `.fit` 文件。

生成方法论参考 SchoolRunV1 改造技术报告（FIT_GENERATOR_TECH_REPORT）：胶囊跑道拟合、横向低频抖动、起终点规则、速度模型、动力学人设、出口长度标定等经验全部移植自 V1 的验证结论。

### 1.1 适用场景

- 校园跑步数据生成（3km / 5km 双档为主）
- 运动 APP 数据导入（Keep / Garmin Connect）
- FIT 文件格式研究

### 1.2 设计目标

- 轻量级：仅一个第三方依赖（fit-tool）
- 易部署：单命令启动，SQLite 文件库自动建表/迁移
- 真实性：轨迹每次按跑道几何生成全新路线（非模板复用）；起点跨次一致、终点自然出跑道；长度误差 <0.1%
- 反"机械化"：速度/步频/功率/触地逐点波动；3km 档采用"快跑段 + 中段慢走(3~4km/h)"强变化剖面（CV≈20~30%）

---

## 2. 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Python `http.server` | 标准库，无需额外安装 |
| 并发模型 | `socketserver.ThreadingMixIn` | 多线程处理并发请求 |
| 数据库 | SQLite | Python 内置，文件级数据库（含 distance 列） |
| 轨迹几何 | 自研 `tools/capsule_track.py` | PCA 拟合、胶囊解析几何、弧长采样、速度曲线 |
| FIT 生成 | `fit-tool` (>=0.9) | Garmin FIT 格式读写库 |
| 前端 | HTML5 + CSS3 + JavaScript | 原生实现，零框架 |
| UI 风格 | Glassmorphism | 液态玻璃效果 + 深色模式 |

---

## 3. 系统架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────┐
│              浏览器 (原生 HTML/CSS/JS)             │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │ 数据填写  │  │ 数据下载  │  │  公告系统     │  │
│  │ (5km/3km │  │ (含距离列) │  │ (V2.0.0beta) │  │
│  │  双档)   │  └──────────┘  └──────────────┘  │
│  └──────────┘                                  │
└────────────────────┬────────────────────────────┘
                     │ HTTP (JSON + 二进制流)
                     ▼
┌─────────────────────────────────────────────────┐
│       ThreadedHTTPServer (端口 5005)              │
│  Handler (do_GET / do_POST / do_OPTIONS)          │
└──────┬──────────────┬──────────────┬────────────┘
       │              │              │
       ▼              ▼              ▼
┌──────────┐  ┌─────────────────┐  ┌──────────┐
│  SQLite  │  │   FIT 生成器      │  │ 公告文件  │
│ activities│  │ capsule_track.py │  │ Call.txt │
│ (+distance)│  │ generate_fit.py  │  │ V2.0.0beta│
│ downloads │  └─────────────────┘  └──────────┘
└──────────┘
```

### 3.2 各层职责

| 层级 | 职责 |
|------|------|
| 前端 | 双档提交/快速提交、表单验证、API 调用、文件下载、公告展示 |
| HTTP 服务器 | 请求路由、静态文件服务、API 处理（含 distance 校验）、CORS |
| 数据层 | SQLite 操作（activities 带 distance 列，旧库 ALTER 迁移）、线程锁 |
| 几何层 | 母版跑道拟合、绕圈采样、横向抖动、速度曲线（capsule_track.py） |
| 生成层 | FIT 消息组装、动力学人设、长度标定（generate_fit.py） |

---

## 4. 目录结构

```
SchoolRunV2/
├── backend/
│   └── server.py              # HTTP 服务器（distance 入库/校验/透传生成）
├── frontend/
│   └── form.html              # 前端单页应用（双档提交 + 下载 + 公告）
├── tools/
│   ├── capsule_track.py       # 胶囊跑道拟合/绕圈/抖动/速度曲线
│   └── generate_fit.py        # FIT 生成编排
├── data/
│   ├── activities.db           # SQLite 数据库（自动生成，含 distance 列）
│   ├── standard.fit            # GPS 母版（跑道拟合用，勿删）
│   └── Call.txt                # 公告内容（首行为版本号）
├── requirements.txt
├── LICENSE
├── .gitignore
├── README.md
└── 技术报告.md
```

---

## 5. 后端模块分析（backend/server.py）

### 5.1 服务器配置

| 配置项 | 值 |
|--------|-----|
| 端口 | 5005 |
| 数据库路径 | `data/activities.db` |
| 并发模型 | `ThreadingMixIn` + `HTTPServer`（daemon_threads） |

### 5.2 API 端点

**GET `/api/activities`**
- 返回全部活动记录（含 `distance`），按 `created_at` 降序

**GET `/api/announcement`**
- 读取 `data/Call.txt`：首行为版本号，其余为正文
- 返回 `{ version: "V2.0.0beta", content: "..." }`

**POST `/api/submit`**
- 请求：`{ user_id, date, start_time, duration, distance? }`
- `distance` 可选：空/缺省 → `null`；否则必须满足 `0 < km <= 100`（非法返回 400）
- 插入 `activities`（含 distance），记录访客 IP
- 返回：`{ success: true, id: N, distance: km|null }`

**POST `/api/generate`**
- 请求：`{ user_id, date, start_time, duration, distance? }`（同上校验）
- 调用 `generate_fit(..., output_path=..., distance=km)` 生成 FIT
- 成功则记录 `downloads`、以二进制流返回、随即删除临时文件

### 5.3 数据库操作

全部经 `db_lock` 保护。`init_db()` 建表后尝试 `ALTER TABLE activities ADD COLUMN distance REAL`（对旧库自动迁移，已存在则忽略）。

```sql
CREATE TABLE IF NOT EXISTS activities (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    date TEXT NOT NULL,
    start_time TEXT NOT NULL,
    duration TEXT NOT NULL,
    distance REAL,               -- 可选：提交时确定的里程(km)
    visitor_ip TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE IF NOT EXISTS downloads (id ..., user_id, visitor_ip, created_at ...);
```

### 5.4 CORS 与异常

- `Access-Control-Allow-Origin: *`；POST/GET/OPTIONS
- POST 全程 try-except，异常 500；参数/距离错误返回 400 与中文提示

---

## 6. 前端模块分析（frontend/form.html）

单页应用：数据填写、数据下载、公告系统三块，Tab 切换。UI 维持玻璃拟态 + 深色模式 + 光源动画。

### 6.1 提交按钮布局（V2.0.0beta）

两行四按钮（均含小字距离标注）：

| 行 | 左（小字 5km） | 右（小字 3km） |
|----|----------------|----------------|
| 快速提交（绿） | 自动编号/时段 + 随机时长(26'00"-35'00") + 距离 `5±U(-0.09,0.4)` | 自动编号/时段 + 随机时长(19'10"-20'53") + 距离 `3±U(-0.09,0.4)` |
| 提交数据（蓝） | 手动填日期时间 + 距离 `5±U(-0.09,0.4)` | 手动填日期时间 + 距离 `3±U(-0.09,0.4)` |

- 距离随机公式：`(基准 + Math.random()*0.49 - 0.09).toFixed(2)`，基准=5 或 3 → 分别落在 4.91~5.40 / 2.91~3.40 km
- 距离随提交入库；下载列表显示实际距离，下载时透传给 `/api/generate`

### 6.2 快速提交时段逻辑（北京时间）

- 15:00-23:59 → 当天，13:30~15:30 随机
- 00:00-14:59 → 前一天，21:00~23:00 随机

### 6.3 表单验证

| 字段 | 正则 |
|------|------|
| `user_id` | `/^\d{3}$/` |
| `date` | `/^\d{8}$/` |
| `start_time` | `/^\d{4}$/` |
| `duration` | `/^\d{4}$/` |

### 6.4 Cookie

- `uid`：记忆编号（365 天）；`ann_ver`：已读公告版本（365 天）

---

## 7. FIT 生成器分析

### 7.1 `tools/capsule_track.py`

- `geo_to_meters / meters_to_geo`：经纬度 ↔ 米制局部坐标（内部统一米制，仅在边界换算）
- `pca_2d`：2×2 解析特征分解求跑道主轴朝向与两正交方向
- `fit_capsule_from_master(path)`：读母版→挑点数最多的一圈→外包络（2%~98% 分位，抗抖动）→胶囊参数（半直道 a、半径 r、单圈 L0）；参数异常降级标准 400m 跑道。母版实测：`a≈41.42m r≈42.61m L0≈433.38m U≈168.1m W≈85.2m`，拟合残差约 29.8m
- `generate_track_loop(...)`：沿弧长等距采样（3m、上限 4000 点）→ 低频平滑横向抖动（触发 25%、峰值≤10m、相关长度 250m）→ 起点前 4 点横向渐变锁定 → 终点最后 45m 横向淡出并沿外侧法向 10~30m 外移 → 实测折线长度标定（含主体偏离 ≤9.95m 硬阻尼与出口二次标定）
- `build_pace_curve / pace_time_fraction`：时间-距离累积曲线（见 7.3）

### 7.2 `tools/generate_fit.py`

签名：`generate_fit(user_id, date, start_time, duration, output_path=None, distance=None)`

处理流程：

1. 解析参数；`distance=None` 时随机 3.00~3.29km（三公里为主），否则 `float(distance)*1000`
2. `fit_capsule_from_master(MASTER_PATH)` 拟合跑道
3. `generate_track_loop` 生成轨迹点 + 每点累计里程
4. 换算经纬度后**整体往正东平移 10m**（仅经度 +1.25e-4°，修正与实地跑道的定位偏差，形状/长度不受影响）
4. 按距离档选速度参数（见 7.3）并构建时间-距离曲线
5. 逐点中心差分（±3 窗 + 物理上限 `head×1.15`）求瞬时速度
6. 组装 FIT：FileId（首条）→ Record ×N → Lap ×圈数 → Session → Activity
7. 写盘前按实测折线长度输出误差报告（目标误差 ≤0.1%）

### 7.3 速度模型

两种模式（`capsule_track.build_pace_curve` 的 `trend_mode`），最后都归一化使总时长精确保持：

**模式一：线性递减 + 每公里随机**（≥4km，5km+ 档）
趋势解析 `s(t)=S·(1-decay·t/T)`，平均速度 = 距离/时长，首端 `S = 平均/(1-decay/2)`；叠加每 seg_m=1km 的独立随机系数 ±amp，再按 smooth_m 平滑。

**模式二：快跑 + 中段慢走**（<4km，3km 档）
速度剖面 = 快跑段 + 中段一次慢走（3~4km/h）。按 距离/总时长 反推可行的快段速度 F 与慢走长度 Lw（`D = F·(T-tw) + W·tw`），快段分前/后两档（前段约 F×1.08、慢走结束后约 F×0.93，整体略越跑越慢）并叠加缓慢速度波动；中段慢走绝对落在 3~4km/h，变化幅度很大（CV≈20~30%）。慢走长度约全程 10%~20%、起点随机落在中段 30%~50% 区间。

| 距离档 | 模式 | 参数 | 效果 |
|--------|------|------|------|
| <4km（3km 档） | 快跑+中段慢走 | 慢走 3~4km/h、占 10%~20%、快段 ≤15.5km/h | 中段出现慢走、快慢差异极大 |
| ≥4km（5km+） | 线性递减 | decay 0.20 + 每公里 ±0.05，平滑 200m | 平缓递减 |

注：平均速度 >9km/h 时首端可能 >10km/h（"总时长优先"策略，5km 档约 26'-35' 时通常仍在合理范围）。

### 7.4 动力学人设（慢跑）

| 指标 | 实现 | 说明 |
|------|------|------|
| 步频 | `cadence=(158+10(k-1)±2)/2` 半值存储，夹取 72~86 | App 显示 ×2 ≈ 154~172，均 158 步/分 |
| 功率 | `160·((0.8+0.2k))³·(1±8%)`，夹取 100~300W | k=瞬时速度/平均速度 |
| 触地 | `39000/(步频)+U(-18,18)`，夹取 200~290ms | 约 247ms 均值 |
| 会话层 | `avg_running_cadence=79`(半值)、`avg_power=160`、`avg_stance_time=247` | Keep 显示 158/160W/247ms |

关键兼容约定（V1 实测结论）：
- **FileId 必须是文件首条消息**（Keep 导入必需）
- 跑步 cadence 按 Garmin 惯例存**半值**
- 时间戳为 FIT 纪元时间（fit_tool 内部毫秒，落盘为纪元秒）
- 记录时间严格单调递增

### 7.5 FIT 消息结构（现版本）

| 顺序 | 消息 | 内容 |
|------|------|------|
| 1 | FileIdMessage | ACTIVITY / GARMIN / time_created |
| 2~N+1 | RecordMessage | 经纬度、累计距离、enhanced_speed、cadence(半值)、power、stance_time、时间戳 |
| … | LapMessage ×圈数 | 每整圈距离=L0×scale，零头圈=剩余距离；起止时间按速度曲线 |
| … | SessionMessage | 总时长/距离、num_laps、卡路里、avg_running_cadence=79、avg_power、avg_stance_time、avg/max_speed |
| 末 | ActivityMessage | num_sessions=1, type=0 |

验证样例（3.00km/21:03）：1001 点、7 圈、实测折线 3000.00~3000.07m；起点经纬度跨次一致 `43.853057,87.699830`；终点出跑道 10.8~24.3m；逐点半值步频 77~81、功率 138~200W、触地 224~269ms；时间跨度精确 1263s。

---

## 8. 数据流程

### 8.1 提交流程

```
用户选择档位/填写 → 前端计算随机距离(仅提交时) → POST /api/submit {…, distance}
    → 校验必填 + distance(0<km≤100 或空)
    → INSERT INTO activities(…, distance)
    → 返回 { success, id, distance }
    → 前端跳转下载页
```

### 8.2 下载流程

```
用户点击下载(行内带 distance) → POST /api/generate {…, distance}
    → 校验 → generate_fit(…, distance)
        → 拟合母版胶囊跑道 → 绕圈采样+抖动+长度标定
        → 速度曲线(按档位参数) → 动力学人设逐点
        → FileId → Records → Laps → Session → Activity → 写盘
    → 返回二进制流 → INSERT INTO downloads → 删除临时文件
（行距离为空/旧记录时按 3.00~3.29km 随机）
```

---

## 9. 公告系统

`data/Call.txt` 首行为版本号，其余为正文（换行保留，前端 `white-space: pre-wrap` 展示）。当前：

```
V2.0.0beta
20260909 0517！！！V2.0.0beta 大版本更新，请务必细读
1.轨迹生成算法全面升级……
```

前端对比 `ann_ver` Cookie：无记录或版本不一致 → 公告按钮黄色呼吸灯闪烁；点击后写 Cookie 停止。

---

## 10. 安全、并发与已知限制

- CORS 全开放，适用于开发/局域网
- SQLite 连接每次新建、由 `db_lock` 串行保护
- 生成的 FIT 以 `data/{user_id}.fit` 命名、发送后即删
- **已知限制**：
  - 无身份认证、无日志文件、无配置文件（端口/路径硬编码）、数据库连接未复用
  - 10km/h 上限与"总时长精确"不可兼得（平均 >9km/h 时首端可 >10km/h）
  - 天气无法写入 FIT，由 App 按开始时间+经纬度反查（开始时间须为过去）
  - 低频横向抖动+长度标定带来约 1~3% 的整体缩放（自洽）
  - 母版非标准 400m 跑道，胶囊按外包络拟合，与真实母版残差约 30m

---

## 11. 依赖项

第三方仅 `fit-tool>=0.9.0`（`requirements.txt`）；其余均为 Python 标准库（http.server / socketserver / sqlite3 / json / threading / os / sys / random / datetime / math）。

---

## 12. 性能与改进建议

- 每次请求动态拟合母版并采样（点数上限 4000，典型 3km≈1000 点、5km≈1700 点），生成耗时秒级
- 后续可做：距离档/速度参数配置化、更细腻 cadence（fractional_cadence）、用户认证、logging、连接池、Docker、单元测试与读回自检

---

*报告版本：V3（同步 V2.0.0beta）*
*生成时间：2026-09-09*
